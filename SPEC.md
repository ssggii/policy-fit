# SPEC — youth-policy

> 무엇을 만드는가. PRD와 충돌 시 PRD가 이긴다.
> 1장(범위표) 작성 완료 — Phase 2 산출. 2·3장은 Phase 3에서 작성.

## 1. MVP 범위표

> 스코프 논쟁 시 이 표가 기준. `구분`의 `↳`는 바로 윗 항목의 변형(기각안·확장변형)을 뜻한다.
> 상태 어휘: in(MVP 포함) / out(제외 확정) / later(지금은 아니되 정해진 게이트로 재검토).

### 1.1 정책 범위

| 정책 | 구분 | 상태 | 사유 |
|---|---|---|---|
| 청년월세 특별지원 (면제군 한정) | 포함 | in | ⓐⓑⓒ 충족. 원가구 소득·재산 평가를 배제하려 면제군으로 한정 (PRD 6장, ADR-0002 D1) |
| 청년 주택드림 청약통장 | 포함 | in | ⓐⓑⓒ 충족 (PRD 6장⑥, ADR-0002 D1) |
| 청년전용 버팀목 전세자금대출 | 포함 | in | ⓐⓑⓒ 충족 (PRD 6장⑥, ADR-0002 D1) |
| 주거급여 청년 분리지급 | 확장후보 | later | 4종째 후보. 신규 원자 2개(부모수급여부·소득인정액 파생) 비용 검토 후 포함 결정 (ADR-0002 D1) |
| 청년월세 면제군 한정 해제 (원가구 자가보고) | ↳ 확장변형 | later | 원가구 자가보고는 가구합산 제외 원칙과 충돌. 확장 시점 재검토 (PRD 6장·8장①, ADR-0002 기각 D1) |
| 청년수당 | 제외 | out | 가구소득 요건으로 자가입력만으로 가/부 단정 불가 (PRD 6장, ADR-0002 D2) |
| 가구(원가구·부부) 합산 요건 정책 일반 | 제외원칙 | out | 자가 입력으로 정확도 담보 불가 → '자가판정 불가' (PRD 6장 일반 원칙, Story 3-2) |
| 추첨·선발·예산소진·모집기간 제약 정책 | 포함(고지) | in | 자격 요건이 아닌 선발 절차·시기 제약 → 판정 대상. 결과에 선발 방식·시기 고지 (PRD 6장·Story 3-2, ADR-0002 D2) |

### 1.2 기능 범위

| 기능 | 구분 | 상태 | 사유 |
|---|---|---|---|
| 일상어 분기 질문 입력 | 핵심 | in | 판정 필요값을 일상어 분기 질문으로 수집 (PRD 6장①·Story 1-1, ADR-0002 D4) |
| 자유 자연어 입력 + LLM 파싱 | ↳ 기각안 | out | 파싱 오류=오판 새 원천, LLM 런타임 의존, 자유 텍스트 개인정보 유입 (PRD 6장①, ADR-0002 기각 D4) |
| 첫 입력만 자유 텍스트 (후보 추리기 한정) 절충안 | ↳ 확장변형 | later | 확장 시점 재검토, 파싱 오류 판정 미유입 구조 전제 (PRD 8장③, ADR-0002 기각 D4) |
| 맞춤 후보 추리기 (MVP 정책 결정론적 필터링) | 핵심 | in | MVP 범위 정책에 대한 결정론적 필터링 (PRD 6장②·Story 1-2) |
| 카탈로그 스케일 후보 추리기 | ↳ 확장변형 | later | 대규모 카탈로그 탐색은 판정 가능/불가 2단 노출 범위 (PRD 8장③) |
| 인증 없는 추정 판정 | 핵심 | in | 무인증으로 자가 입력값을 요건에 대입·판정 (PRD 6장③·Story 1-3, H4) |
| 근거 투명 공개 (충족/미충족 조건 + 출처·기준연도) | 핵심 | in | 항목별 근거 제시, 행정 용어 풀이 (PRD 6장④·Epic 2) |
| 확정으로의 안전한 핸드오프 (추정 고지 + 공식 채널 링크) | 핵심 | in | 추정 명시 + 신청 채널 연결 (PRD 6장⑤·Epic 3) |
| 4-state 판정 결과 (가능/부적합/추가확인필요/자가판정불가) | 핵심 | in | 흑백 단정 대신 4상태. 의미론 상세는 DOMAIN 소유 (PRD 7장, Phase 4 이관) |
| H3 선제 알림 / 구독 | 확장후보 | later | MVP 밖, 로드맵 유지. 핵심 후행지표 통과 후 단계적 도입 (PRD 4장 H3·8장②) |
| 판정 가능/불가 2단 노출 (Tier 1/2) | 확장후보 | later | catalog coarse 정규화 필요, 핵심 후행지표 통과 후 (PRD 8장③) |
| 정책 범위 확장 (교육·금융·참여 등) | 확장후보 | later | 미커버 수요 지표 순위 근거로 순차 확장 (PRD 8장①) |
| 유효성 인식 계측 | 제외 | out | MVP 계측 외 — 설문·피드백 경로 도입 시점부터 (PRD 5장) |

## 2. 기능 목록

> 각 F-ID는 SPEC 1.2의 `in` 기능이자 PRD 7장 Epic/Story의 이관본이다.
> ID는 영구·append-only. 폐기돼도 재사용하지 않는다. 수용 기준은 테스트 러너가 케이스로 변환 가능한 관찰형으로 쓴다.
> 4-state enum의 의미 정의는 DOMAIN이 소유하고, 아래 F는 그 상태를 제시·구분하는 *행동*만 규정한다.

### Epic 1 — 정책 목록만 받던 사용자가, 공공 인증 절차 없이 자신의 자격 가부를 근거와 함께 즉시 확인하게 된다.

#### F-001. 일상어 분기 질문 입력

- 설명: 판정 필요값을 행정 용어 없이 일상어 분기 질문으로 하나씩 수집한다. (PRD Story 1-1)
- 수용 기준:
  - [ ] 판정 필요값(나이·무주택 여부·임차 형태·세대 독립성·본인 소득·본인 재산) 각각에 대응하는 질문이 존재한다
  - [ ] 앞선 답변으로 불필요해진 질문은 노출되지 않는다 (예: 무주택=아니오 → 임차 형태 질문 skip)
  - [ ] 각 질문은 행정 용어를 그대로 요구하지 않는다 (일상어 + 보조 설명)
  - [ ] 세대 독립성 질문이 본인 소득 질문보다 먼저 제시된다
  - [ ] 세대가 부모와 동일한 것으로 응답되면, 부모 소득이 판정에 포함됨을 질문 또는 보조 설명으로 고지한다
  - [ ] 모든 값은 '모름'/'대략'으로 응답 가능하며, 그 경우 결과에 '불확실'이 연동 표기된다

#### F-002. 맞춤 후보 추리기

- 설명: 입력값으로 해당 가능성이 있는 정책만 좁혀 제시하고, 후보에서 판정으로 잇는다. (PRD Story 1-2)
- 수용 기준:
  - [ ] 입력값에 해당 가능성이 없는 정책은 후보에 노출되지 않는다 (동일 입력 → 후보 집합이 결정론적으로 유일)
  - [ ] 후보는 MVP 범위 정책(SPEC 1.1의 in) 안에서만 산출된다
  - [ ] 각 후보에서 판정(가/부 확인)으로 이동하는 액션이 같은 화면에 제공된다
  - [ ] 후보 목록이 종착 화면이 아니다 (판정 진입 경로가 후보와 동일 흐름에 존재)

#### F-003. 인증 없는 추정 판정 + 결과 제시

- 설명: 본인인증 없이 자가 입력값을 요건에 대입해 사용자 대상 3-state로 결과를 제시한다. (PRD Story 1-3)
- 수용 기준:
  - [ ] 본인인증·로그인·회원가입 없이 판정 결과 화면에 도달한다
  - [ ] 사용자 대상 결과는 가능 / 부적합 / 추가 확인 필요 3상태 중 하나로 표시된다
  - [ ] '자가판정 불가'는 이 화면의 사용자 자격 결과로 표시되지 않는다 (정책 범위 문제는 F-007 소관)
  - [ ] 입력에 '모름/대략'이 포함되면 결과가 '추가 확인 필요'와 연동된다

### Epic 2 — 사용자가 판정 결과를 그대로 받아들이는 데 그치지 않고, 왜 그렇게 나왔는지 이해하고 신뢰하게 된다.

#### F-004. 판정 근거 투명 공개

- 설명: 가/부를 가른 조건을 항목별로 제시하고 행정 용어를 그 자리에서 풀며, 출처·기준 연도를 표기한다. (PRD Story 2-1)
- 수용 기준:
  - [ ] 판정 결과에 가/부를 가른 요건이 충족·미충족으로 항목별 구분되어 표시된다
  - [ ] 결과에 등장하는 행정 용어는 그 자리에서 풀이가 제공된다 (재검색 불필요)
  - [ ] 각 요건에 출처와 기준 연도가 표기된다

#### F-005. 추정 고지 상시 노출

- 설명: 모든 판정 결과에 추정임과 "확정은 공식 신청에서 확인"을 상시 노출한다. (PRD Story 2-2)
- 수용 기준:
  - [ ] 모든 판정 결과 화면(가능/부적합/추가 확인 필요)에 추정 고지가 노출된다
  - [ ] "확정은 공식 신청에서 확인" 안내가 함께 노출된다
  - [ ] 추정 고지는 조건부가 아니라 상시 노출된다

### Epic 3 — 사용자가 판정에서 멈추지 않고, 막힘 없이 실제 신청으로 넘어간다.

#### F-006. 공식 신청 채널 핸드오프

- 설명: 신청 유도 대상 결과를 공식 채널로 한 번에 잇고, 그 지점을 전환 계측점으로 삼는다. (PRD Story 3-1)
- 수용 기준:
  - [ ] '가능' 또는 '추가 확인 필요' 정책에 공식 신청 채널 링크가 제공된다
  - [ ] '부적합' 정책에는 신청 유도 링크가 제공되지 않는다
  - [ ] 신청 채널 이동은 단일 액션으로 이뤄진다 (중간 인증·추가 입력 없이)
  - [ ] 이 이동 지점에서 신청 전환 이벤트가 계측된다 (PRD 5장 신청 전환율 측정 지점)

#### F-007. 자가판정 불가 정책의 정직한 제외 + 선발 절차 고지

- 설명: 가구 합산 등 자가 입력으로 담보 불가한 요건 정책은 '자가판정 불가'로 제외하고, 선발 절차 제약 정책은 판정하되 고지한다. (PRD Story 3-2)
- 수용 기준:
  - [ ] 가구(원가구·부부) 소득·재산 합산 등 자가 입력으로 정확도를 담보할 수 없는 요건 정책은 '자가판정 불가(범위 밖)'로 표시되고 판정 대상에서 제외된다
  - [ ] '자가판정 불가'는 '부적합'과 문구·표시상 구분된다 (전자=시스템 한계, 후자=사용자 자격 결과)
  - [ ] 임계가 느슨해 대부분 충족하는 요건(예: 본인 재산)은 '모름' 응답에도 판정을 중단하지 않고, 해당 요건을 불확실 표기한 채 '추가 확인 필요'로 진행한다
  - [ ] 추첨·선발·예산 소진·모집기간이 있는 정책은 판정 대상에서 제외되지 않는다
  - [ ] 그런 정책의 결과에는 선발 방식(추첨·선착순·심사 등)과 신청 시기가 함께 고지된다
  - [ ] 요건별 '모름' 처리 규칙의 상세는 본 F가 아니라 요건 원자 spec(DOMAIN)이 소유한다 (본 F는 UX 행동만 규정)

## 3. 참고

- 판정 로직 상세: `DOMAIN.md`
- API 형식: `contracts/openapi.yaml`

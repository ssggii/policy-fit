spring.application.name=backend

# contracts/*.schema.json, openapi.yaml는 모든 요청 스키마에 additionalProperties:false를 명시한다.
# Jackson 기본값이 이미 true이나(Boot가 끄지 않음), Boot/Jackson 버전업으로 기본값이 바뀌어도
# 계약을 강제하도록 명시적으로 고정한다.
spring.jackson.deserialization.fail-on-unknown-properties=true


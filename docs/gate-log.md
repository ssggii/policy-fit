# 게이트 차단 로그

> `loop.md` §8.5 관측. 게이트가 실제로 작동했음을 보여주는 기록이자, `loop.md` §9 회고의 3회 누적/반복 차단 판단 근거.
> 차단이 발생할 때마다 커밋하지 않는다 — 작업 사이클(세션) 동안 누적했다가, 그 사이클의 최종 커밋 직전에 한 번에 append한다.

| 날짜 | 게이트 | 사유 | 작업 | 참고 |
|---|---|---|---|---|
| 2026-07-27 | 3 | `IncomeSelfAtomEvaluator`가 `approx=true`를 DOMAIN §3.4를 위반해 `boundary`로 태깅(실제는 `input_uncertain`) — 코드 주석도 "DOMAIN 확정 규칙"이라 잘못 표기 | 백엔드 판정 엔진, 청년 주택드림 청약통장 (F-003) | consistency-reviewer가 발견, 같은 세션에서 즉시 수정 (커밋 `7b9909d`) |
| 2026-08-03 | C | freeze 훅이 `contracts/openapi.yaml`(married 필드·enum 확장)의 에이전트 직접 편집을 차단 — 계약 변경은 사람 승인·편집 필요 | 청년월세 범위 게이트 (F-003/F-007, #10) | 사람이 검토 포인트 승인 후 직접 편집해 해소, 에이전트가 diff 정합성 검증 |
| 2026-08-08 | 3-1 | code-reviewer가 #42 초기 구현에서 PolicySelect·정책 카탈로그가 CheckFlow에 미연결(죽은 코드·AC1 미충족) + CollectedAnswers 타입 안전성 후퇴를 지적 | 웹 정책 선택·입력 질문 확장 (F-001, #42) | 스코프 A(wiring 포함)로 재확인 → CheckFlow wiring + 타입 강화 + 테스트 보강, 재리뷰에서 해소 |
| 2026-08-08 | 3-1 | code-reviewer가 #22 디자인 반영에서 (a) unlayered `:focus-visible{border-radius}`가 포커스 시 라운드 요소 붕괴(Playwright 실측) (b) `/check` h1 소실(a11y)을 지적 | 웹 디자인 반영 (F-001, #22) | SSOT를 그대로 옮긴 focus 규칙의 Tailwind cascade-layer 부작용 + h1 회귀 → radius 제거·화면별 h1 승격·F-005 대비 상향으로 수정, 재리뷰 통과 |
| 2026-08-08 | 3-1 | code-reviewer가 #12에서 커밋 `Refs: F-ID` 푸터 누락·브랜치 `<F-ID>` 누락·회귀 단언 부족(컨벤션) 지적 | 웹 out_of_scope 표시 경계 (F-003/F-007, #12) | 커밋 Refs 푸터·의도 주석·테스트 단언 반영으로 해소. **F-ID 추적(커밋 푸터·브랜치)은 #10·#42·#22에서도 반복 누락된 컨벤션 갭 → 회고에서 enforce/완화 일괄 결정 대상** |
| 2026-08-12 | 3-1 | code-reviewer가 #17(골든셋·baseline)에서 "PRD.md가 backend-only 스코프 커밋에 사람 승인 없이 섞였다"고 BLOCK — 실제로는 같은 세션 대화에서 diff 제안 후 승인받고 반영한 것으로, 리뷰어 서브에이전트가 대화 맥락(승인 여부)을 볼 수 없어 생긴 오탐 | 골든셋 확충 + FP baseline (#17) | 오탐으로 판단하고 그대로 진행(핫픽스 아님) + 리뷰어가 부수적으로 짚은 "PRD·backend 변경을 커밋 분리"는 타당해 반영. **패턴 후보**: 제품문서 동반 티켓에서 reviewer 프롬프트에 "이미 사람 승인 받음" 맥락을 명시하지 않으면 반복될 수 있음 — 1회, 승격은 다음 재발 시 판단 |
| 2026-08-14 | 3 | consistency-reviewer가 `meta.source`에 대조 일자·한계 서술·이슈 번호를 추가한 것을 ADR-0008 D2 위반으로 BLOCK — `meta.source`는 `AtomReasoning.source`를 거쳐 결과 화면 "출처:"로 사용자에게 노출되며, D2가 그 용도를 내부 `provenance` 형제 필드로 분리해 뒀다(D2가 명시적으로 기각한 대안을 그대로 실행) | income_self 환산 규정 출처 확정 (#14) | 노출 경로를 직접 확인해 유효 판정 → 정책 JSON 2개 `meta.source` 변경 전량 원복, 인용문은 클래스 주석에 임시 보관하고 #76에 인계 코멘트. **ADR을 쓴 다음 날 그 ADR이 기각한 대안을 실행한 사례** |
| 2026-08-14 | 3-1 | code-reviewer가 클래스 주석의 "증빙이 원천징수영수증 등 연간 확정액이라"를 근거 없는 확정 서술로 BLOCK — 확보한 tier 1 인용문에 없고 tier 3 검색 요약에서 온 내용을, 같은 작업에서 "인용하지 않겠다"고 선언한 직후 산문에 섞음 | income_self 환산 규정 출처 확정 (#14) | 1차 출처(제출서류 페이지) 추가 확보로 정정. 재확인 결과 서술 **방향도 반대**였음(증빙이 연간 확정액 고정이 아니라 급여내역 경로가 택1로 열려 있고, 연간 확정액이면 ×12보다 작아지는 게 아니라 커짐). 골든 픽스처 옛 표현 갱신도 함께 지적 |

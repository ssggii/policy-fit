# loop — 작업 루프 정의

> 에이전트가 매 작업에서 따르는 표준 사이클의 운영 규칙. 책임 범위는 **1~7 (스펙 → commit)** + 머지 후 정리(§2).
> push 이후(PR·CI)는 결정론 영역이다 — **원격이 이미 연결·활성 상태**(§2 "머지 후" 참조). 배포(CI 이후)는 미결(ADR-0004 D6).
> 작업 단위·게이트·모델·멀티에이전트·상태·실패 처리를 정의한다. 갱신은 회고(9)를 통한다.

## 1. 작업 단위와 등급

- **작업 단위 = SPEC 기능 ID(F-001…) 1개**가 기본. 기능이 아니면(설정·문서·리팩터) 명시적 chore로 다룬다. 단위가 크면 F-ID를 하위 작업으로 분할한다.
- **작업 등급**으로 게이트 적용 범위를 조절한다(트리비얼 과적용 방지):

| 등급 | 예 | 적용 게이트 |
|---|---|---|
| **A 경량** | 문서·설정·오타 | S(경량)·4 (+해당 시 2). 3·3-1 생략 |
| **B 일반** | 기능 구현·버그 수정 | 1~7 전부 |
| **C 위험** | 계약·경계·스키마·보안·비가역 | B + 게이트 1(plan)·C 강제 + Opus 검토 |

- 티켓 착수 직후 **등급과 적용/생략 게이트를 한 줄로 선언**하고 시작한다(이슈 없이 시작하는 chore·fix 포함). 등급이 게이트 1·3·3-1의 소환 여부를 가르므로, 선언해두면 오분류를 착수 시점에 잡을 수 있다.
- 선언은 PR 본문 체크리스트에도 남겨 세션 밖에서 추적 가능하게 한다 — 대화에만 있으면 다음 세션·감사 시점에 확인할 방법이 없다.

## 2. 표준 사이클 (1~7)

```
1. 스펙          F-ID·완료기준(=SPEC 수용기준) 확정       ▸[S]  사람 승인
2. 설계 검토      ARCHITECTURE·DOMAIN·contracts 로드       ▸[0]  설계 먼저(자기점검)
3. 코드 구현      스코프 분기 → 설계대로 작성
     ├▸[B]  현재 브랜치 확인(≠main) — main이면 구현 중단, 브랜치 생성+즉시 push 후 재개
     ├ 스코프 판정: backend-only / web-only / 풀스택
     ├▸[C]  contracts/ 변경 필요? → 중단·사람 라우팅(계약 고정)
     ├▸[D]  외부 라이브러리 필요? → 사람 승인
     └▸[1]  위험 변경(등급 C)은 plan mode 먼저(Opus 검토)
4. 테스트        경로별 러너(JUnit / Vitest·Playwright)    ▸[2]  green +(eval 임계값, Phase7~) 아니면 정지(자기점검)
5·6. 정합성+리뷰  reviewer 서브에이전트                     ▸[3]  설계·계약 경계 위반 0
                                                         ▸[3-1] 중대 이슈 0
7. commit        feature 브랜치 · Conventional Commits     ▸[4]  사람 diff·메시지 검토
──────────────────────────────────────────────
  ↺ 리뷰(5·6) 지적 → 3.구현 복귀 → 4.테스트 재실행
  │ push → PR → CI  (원격 연결 후 활성)
  ▼ 8.배포(미결 — ADR-0004 D6) → 9.회고 → 다음 사이클
```

**풀스택 작업**은 **계약 확정 먼저** → backend·web을 각 워크트리에서 병렬 구현(§5) → **통합(E2E)까지 통과**해야 7로 진입한다.

**머지 후** (squash merge는 origin/main에 새 커밋을 만들어 로컬과 갈라진다, CONTRIBUTING.md):

1. 로컬 동기화 — `git fetch origin && git checkout main && git reset --hard origin/main && git remote prune origin` (항상, `Closes`/`Refs` 무관)
2. 이슈 완료조건 체크박스 갱신 — `Closes`로 자동 닫혀도 GitHub은 본문 체크박스를 안 건드린다(항상 수동)
3. 이슈 close — **`Refs #N`으로 연 PR만** 완료 코멘트와 함께 수동 close. `Closes #N`이었으면 머지 시 이미 자동으로 닫혔으니 생략
4. (선택) 보드 Status 확인

게이트 3·3-1은 병렬 실행한다. 저위험 권고는 그 자리에서 즉시 반영 후 테스트 재실행으로 재검증한다. 구조적 원인의 중대 이슈는 핫픽스하지 않고 §8.2 fallback(회고 등록 + 구현 복귀)을 따른다.

## 3. 게이트 규칙

| 게이트 | 통과 조건 | 강제 주체 | 구현 위치 |
|---|---|---|---|
| **S** | F-ID 매핑 + 완료기준(=SPEC 수용기준) 명시·승인 | 사람 | `skills/spec` + 사람 |
| **0** | ARCHITECTURE·DOMAIN·contracts를 먼저 읽고, 스코프 관련 accepted ADR의 결정사항이 실제 코드에 반영됐는지 확인(문서상 accepted ≠ 구현 완료) | 에이전트 자기점검(soft) | CLAUDE.md 규칙 |
| **B** | 구현 시작 전 현재 브랜치가 `main`이 아님을 확인(CONTRIBUTING.md 브랜치 규칙) | hook(하드 차단) | `hooks/guard-branch.sh` (PreToolUse) |
| **C** | 작업이 `contracts/` 변경을 요구하면 impl 중단, 사람 승인 라우팅 | 사람 | CLAUDE.md DO NOT + 진입 체크 |
| **D** | 외부 라이브러리 추가 전 승인 | 사람 | CLAUDE.md DO NOT + hook(가능 시) |
| **1** | 위험 변경(등급 C)은 계획 사전 검토 | 사람 | plan mode |
| **2** | 경로별 테스트 green +(LLM 파싱 시 eval 임계값, Phase7~) | 에이전트 자기점검(soft) | 러너 직접 실행(`./gradlew test`·`pnpm test`). **hook 미구현 — 기계 강제 없음** |
| **3** | ARCHITECTURE·DOMAIN의 경계·계약 위반 0, 판단 로직 런타임 LLM 누수 0 | reviewer agent | `agents/consistency-reviewer.md` |
| **3-1** | 중대 이슈(버그·보안·가독성·컨벤션·엣지) 0 | reviewer agent | `agents/code-reviewer.md` (기존 `code-review` 스킬 래핑 가능) |
| **4** | diff·메시지 검토 후 승인 + 테스트 green | 사람 | 커밋 전 사람 diff 검토. **hook 미구현 — 기계 강제 없음** |

- 게이트 **3은 특정 아키텍처 스타일을 전제하지 않는다**(헥사고날 등은 미결). 백엔드 아키텍처를 ADR로 확정하면 그때 게이트를 특화한다.
- 게이트 **0은 자기점검(soft)** 이라 기계 강제 불가 — 중요 시 reviewer가 "설계 미참조" 신호로 반려해 보강한다.

## 4. 모델 정책

- **Haiku 4.5** — 단순·반복·저위험(보일러플레이트, 짧은 조사).
- **Sonnet 5** — 기본 워크호스(스펙·구현·정합성·수정·리뷰). 메인 세션·리뷰어 기본값.
- **Opus 5** — 복잡한 아키텍처 결정·모호 케이스 검증에만 선별. 게이트 1/등급 C에서 수동 전환(`/model`).
- (옵션) **Fable 5** — 빠른 저비용 조사·보일러플레이트 대안.

## 5. 멀티에이전트 토폴로지

- **메인 에이전트**: 1~3(스펙·설계검토·구현). 맥락 연속성을 위해 한 세션에서 유지한다.
- **구현 분기(풀스택)**: backend 에이전트(Java/`backend/`)·web 에이전트(Next/`web/`)를 각 **git worktree**에서 병렬 실행. `contracts/`는 **읽기 전용·고정**(누구도 구현 중 변경 금지 → 필요 시 게이트 C). **통합(E2E) 전 commit 금지.**
- **서브에이전트**: `consistency-reviewer`(게이트3)·`code-reviewer`(게이트3-1)·`research`(Haiku)·`test`(러너 실행·대용량 로그 격리).
- 정합성(3)과 리뷰(3-1)는 차단 사유를 분리 보고하되 호출은 묶을 수 있다. 구체 구성은 `agents/`에서 확정(Phase 6f).

## 6. 루프 상태·재개

- 각 작업을 하네스 Task로 등록하고 **TaskUpdate로 현재 단계·게이트 통과 이력·재시도 카운트**를 갱신한다.
- 컨텍스트 압축·세션 재시작 시 TaskList/TaskGet로 "어느 단계·어느 게이트"인지 복원해 이어간다.
- 게이트 차단은 사유와 함께 작업 Task 노트에 남긴다(→ §8.5 카운팅·§9 승격의 입력).

## 7. 환류 (cycle-back)

- 리뷰(5·6) 지적 → **3.구현 복귀** → 수정 → **4.테스트 재실행** → 다시 5·6.
- 같은 게이트에서 2회 연속 차단되면 §8 fallback으로 전환한다.

## 8. Fallback — 게이트가 통과하지 못할 때

### 8.1 재시도 예산
- 동일 게이트 자동 재시도 **최대 2회**. 3번째부터 자동 진행 중단·사람 보고.
- **테스트를 느슨하게 고쳐 green을 만드는 것은 금지** — 즉시 사람 에스컬레이션.

### 8.2 게이트별 fallback

| 게이트 | 막혔을 때 | 행동 |
|---|---|---|
| S | 완료 기준 모호 | 진행 중단. 모호 항목을 질문 목록으로 사람 확정 요청. 추측 확장 금지 |
| 0 | 설계 문서 없음/불일치 | 구현 전 정지. 설계 부재면 "설계 먼저" 작업으로 분리 제안 |
| C | 계약 변경이 불가피 | 차단 유지. 계약 변경 제안(diff)을 사람 승인받은 뒤에만 재개 |
| D | 필요한 라이브러리가 승인 안 됨 | 대안(표준 라이브러리) 검토 또는 사람 승인 요청. 무단 추가 금지 |
| 1 | 위험 변경 | plan mode 전환, 계획만 제시하고 승인 대기. Opus 검토 |
| 2 | 2회 수정 후에도 red | 정지. 실패 테스트·원인 가설·시도 요약 보고. 테스트 완화 금지 |
| 2(eval) | 임계값 미달 | 근거 제시 후 (a)구현 보강 (b)임계값 재합의 중 선택 요청 |
| 3 | 경계 위반이 설계상 불가피 | 차단 유지. "설계 변경 필요" 신호로 격상 → 게이트 S/0 회귀 |
| 3-1 | 중대 이슈가 구조적 원인 | 핫픽스 금지. 회고(9) 항목 등록 + 구현(3) 복귀 |
| 4 | 테스트 green 아님 | 사람이 차단. 7 진입을 막고 4로 되돌림 |

### 8.3 에스컬레이션 트리거 (자동 진행 중단 + 사람 호출)
1. 재시도 예산 소진
2. 통과를 위해 스펙/설계/계약/테스트를 바꿔야 함(= 상위 게이트 문제)
3. 보안·데이터 손실·비가역 작업(삭제·권한·배포·계약 변경)
4. 고비용 모델 전환(Opus)이 필요한 판단

### 8.4 degraded mode (hook 자체 실패)
- 러너·`jq` 등 도구가 환경 문제로 실패하면 hook을 **거짓 green으로 통과시키지 말 것**. 도구 부재 확인 시 사람에게 복구 요청, 복구 전까지 commit 게이트는 수동 확인으로 대체.

### 8.5 관측
- 게이트 차단 1건당 `docs/gate-log.md`에 **1줄(날짜·게이트ID·사유·작업)** 을 기록한다. 매 차단마다 커밋하지 않고, 작업 사이클(세션)의 최종 커밋 직전에 그 사이클 동안 쌓인 항목을 일괄 append한다. §9 승격 판단의 카운트 근거이자, 게이트가 실제로 작동했음을 보여주는 기록.

## 9. 회고 (9) → 루프 갱신

회고는 **2단**이다 — 관찰은 신선할 때(티켓), 승격은 누적이 보일 때(스프린트).

**① 티켓(사이클) 단위 — 관찰 캡처만**
- 매 사이클 종료 시 관찰만 남긴다: 게이트 차단은 `docs/gate-log.md`(§8.5), 반복된 판단·부족했던 정보·자동화 지점은 프로젝트 메모리 한 줄.
- full `/retro`나 규칙 승격은 하지 않는다 — "3회" 기준은 한 티켓으로 판정 불가하므로 과승격 방지.

**② 스프린트 단위 — 승격 판정 (full `/retro`)**
- 스프린트 종료 시 `skills/retro`로 티켓별 관찰 + `gate-log` 전체를 스프린트 범위로 집계·판정한다.
- 같은 교정 **3회 누적** → 규칙 승격(CLAUDE.md·CONTRIBUTING 등, /retro 경유 diff 제안).
- 같은 게이트 **반복 차단**(§8.5 카운트) → 해당 hook·리뷰어 기준을 이 파일에서 갱신.
- **예외**: 스프린트 중 같은 게이트가 반복 차단되면 종료를 기다리지 말고 즉시 spot 회고.
